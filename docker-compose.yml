services:

  # postgres:
  #   image: postgres:16-alpine
  #   container_name: financial-records-postgres
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   environment:
  #     - POSTGRES_DB=financial_records
  #     - POSTGRES_USER=financial_user
  #     - POSTGRES_PASSWORD=financial_password
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data

  mongodb:
    image: mongo:7
    container_name: financial-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: financial_records
      MONGO_INITDB_ROOT_USERNAME: mongo_user
      MONGO_INITDB_ROOT_PASSWORD: mongo_password
    volumes:
      - mongodb-data:/data/db

  # mongo-express:
  #   image: mongo-express:1.0.2
  #   container_name: financial-mongo-express
  #   restart: unless-stopped
  #   depends_on:
  #     - mongodb
  #   ports:
  #     - "8081:8081"
  #   environment:
  #     ME_CONFIG_MONGODB_SERVER: mongodb
  #     ME_CONFIG_MONGODB_PORT: 27017
  #     ME_CONFIG_MONGODB_ADMINUSERNAME: mongo_user
  #     ME_CONFIG_MONGODB_ADMINPASSWORD: mongo_password
  #     ME_CONFIG_BASICAUTH: "false"

  n8n:
    image: n8nio/n8n:latest
    container_name: financial-n8n
    platform: linux/amd64
    environment:
      WEBHOOK_URL: https://n8n.nyx-agent.com
      N8N_HOST: n8n.nyx-agent.com
      N8N_PROTOCOL: https
      GENERIC_TIMEZONE: America/Sao_Paulo
      N8N_LOG_LEVEL: debug
      N8N_COMMUNITY_PACKAGES_ALLOW_TOOL_USAGE: true
    volumes:
      - n8n_data:/home/node/.n8n
    ports:
      - "5678:5678"

volumes:
  postgres-data:
  mongodb-data:
  n8n_data:
  waha_sessions:
  waha_media:
